.container-fluid
  .row.row_striped.row_log.row_header
    .col-md-12 Фраза
  - phrases.each do |phrase|
    .row.row_striped.row_log{id: "phrase#{phrase.id}"}
      .col-md-8= phrase.value
      .col-md-2= link_to 'Редактировать', edit_total_phrase_path(id: phrase.id, redirect_to: request.fullpath), remote: true
      .col-md-2= link_to 'Удалить', total_phrase_path(id: phrase.id, redirect_to: request.fullpath), method: :delete, data: { confirm: "Уверенны? Удалить: '#{phrase.value}'?" }
