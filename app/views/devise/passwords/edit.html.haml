.col-md-4.col-md-offset-4
  .form-sign-in
    .alert.alert-secondary
      %h4.text-muted Поменяйте ваш пароль
      = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f|
        = devise_error_messages!
        = f.hidden_field :reset_password_token
        .form-group
          = f.label :password, "Новый пароль"
          %br/
          - if @minimum_password_length
            %em
              (#{@minimum_password_length} символов минимум)
            %br/
          = f.password_field :password, autofocus: true, autocomplete: "off", class: 'form-control', required: true
        .form-group
          = f.label :password_confirmation, "Подтвердите новый пароль"
          %br/
          = f.password_field :password_confirmation, autocomplete: "off", class: 'form-control', required: true
        .actions
          = f.submit "Изменить свой пароль", class: 'btn btn-primary'
